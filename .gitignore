# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Jetbrain
.idea
settings.local.json

# Render test output (generated, not committed)
render/testdata/output/
.worktrees
